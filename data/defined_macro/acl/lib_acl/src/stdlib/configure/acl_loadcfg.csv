id,name,value
463a6d74-325f-4cbc-97df-af3d2d01812e,ERETURN,"do {                                                      \
	if (a)                                                               \
		acl_array_destroy(a, acl_myfree_fn);                         \
	if (cfg_line) {                                                      \
		if (cfg_line->value)                                         \
			acl_myfree(cfg_line);                                \
		acl_myfree(cfg_line);                                        \
	}                                                                    \
	return (x);                                                          \
} while (0);"
ab7180df-fef1-469f-829e-794300ee84bb,ERETURN,"do { \
	if (content_buf != NULL) \
		acl_myfree(content_buf); \
	if (filefd != ACL_FILE_INVALID) \
		acl_file_close(filefd); \
	if (parser != NULL) { \
		acl_array_destroy(parser->_cfg_array, NULL); \
		acl_myfree(parser); \
	} \
	return (x); \
} while (0);"
0889a423-7504-4c48-bfa3-ea83424bba76,RETURN,"do { \
	if (content_buf != NULL) \
		acl_myfree(content_buf); \
	if (filefd != ACL_FILE_INVALID) \
		acl_file_close(filefd); \
	return (x); \
} while (0);"
e13f778d-2005-4c43-8274-a0a491fd9a65,SKIP,for (; *var && (cond); var++) {}
cd8339c1-02f1-4ce1-927f-2e355e04a0b2,RETURN,"do { \
	if (filefd != ACL_FILE_INVALID) \
		acl_file_close(filefd); \
	return (x); \
} while (0);"
ca958168-f515-4559-8f9d-7996dbf4df92,ERETURN,"do { \
	if (cfg_line == NULL) \
		return (x); \
	if (cfg_line->value == NULL) { \
		acl_myfree(cfg_line); \
		return (x); \
	} \
	for (i = 0; i < cfg_line->ncount; i++) { \
		if (cfg_line->value[i] == NULL) \
			break; \
		acl_myfree(cfg_line->value[i]); \
	} \
	acl_myfree(cfg_line->value); \
	acl_myfree(cfg_line); \
	return (x); \
} while (0);"
